server:
  port: 8081
  servlet:
    context-path: /aimin-auth

spring:
  application:
    name: aimin-auth

  profiles:
    active: dev

  config:
    import:
      - nacos:${spring.application.name}-${spring.profiles.active}.yml
      - classpath:base.yml
      - classpath:redis.yml
      - classpath:cache.yml
      - classpath:ds.yml


wx:
  appid: wx9568bdfc5d43d6ed
  secret: 1d623de8e508759374afe10b888fb122

